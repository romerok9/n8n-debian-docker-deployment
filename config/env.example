# PostgreSQL Configuration
POSTGRES_USER=n8n_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
POSTGRES_DB=n8n_database

# n8n Configuration
N8N_ENCRYPTION_KEY=GENERATE_RANDOM_32_CHAR_KEY_HERE
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=CHANGE_THIS_ADMIN_PASSWORD

# Domain Configuration
N8N_HOST=n8n.yourdomain.com

# Timezone Configuration
# Examples: America/New_York, Europe/Madrid, America/Bogota
GENERIC_TIMEZONE=UTC

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# IMPORTANT SECURITY NOTES:
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
#
# 1. Copy this file to .env and update all values
#    $ cp env.example .env
#
# 2. Generate strong passwords (at least 16 characters)
#    $ openssl rand -base64 32
#
# 3. NEVER commit the .env file to git
#    (it's in .gitignore by default)
#
# 4. Keep this file secure - it contains sensitive data
#
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

